# 课后作业 思考题
## 1. 代数证明：一元线性回归残差性质
一元线性回归模型：
$$y_i=\beta_0+\beta_1 x_i+\varepsilon_i$$
拟合方程：
$$\hat y_i=\hat\beta_0+\hat\beta_1 x_i$$
残差定义：
$$e_i=y_i-\hat y_i$$

### 证明1：$\sum_{i=1}^n e_i = 0$
最小二乘的目标：最小化 $S=\sum_{i=1}^n (y_i-\beta_0-\beta_1 x_i)^2$
对截距$\beta_0$求偏导并令导数等于0：
$$
\frac{\partial S}{\partial \beta_0}= -2\sum_{i=1}^n(y_i-\hat\beta_0-\hat\beta_1 x_i)=0
$$
$$
\sum_{i=1}^n(y_i-\hat y_i)=\sum_{i=1}^n e_i=0
$$

### 证明2：$\sum_{i=1}^n x_i e_i = 0$
对斜率$\beta_1$求偏导并令导数等于0：
$$
\frac{\partial S}{\partial \beta_1}= -2\sum_{i=1}^n x_i(y_i-\hat\beta_0-\hat\beta_1 x_i)=0
$$
代入残差定义 $e_i=y_i-\hat y_i$：
$$
\sum_{i=1}^n x_i e_i = 0
$$
> 说明：以上两个式子就是最小二乘的正规方程（normal equation）。

---

## 2. 思考题：新增无关随机特征，$R^2$ 上升，调整$R^2_{adj}$下降
### 公式回顾
$$
R^2=1-\frac{RSS}{TSS},\quad R^2_{adj}=1-\frac{RSS/(n-p-1)}{TSS/(n-1)}
$$
- $RSS$：残差平方和
- $TSS$：总平方和（固定不变，只和$y$有关）
- $n$：样本量，$p$：自变量个数

### 解释
1. **$R^2$**：只要新增任何自变量（哪怕是完全随机无关变量），最小二乘一定会**降低或者不变RSS**，所以 $R^2=1-RSS/TSS$ 一定上升或不变。$R^2$**不考虑自由度惩罚**，自变量越多，它越虚高。
2. **$R^2_{adj}$**：对自由度做惩罚。增加变量，$p$变大，分母自由度 $n-p-1$ 变小。
    - 如果新增变量对模型几乎没有解释力：RSS下降幅度很小，但是自由度惩罚项 $\frac{1}{n-p-1}$ 增大。
    - 惩罚带来的负面效果 > RSS下降带来的正面效果，于是 $R^2_{adj}$ **下降**。
> 一句话总结：$R^2$ 不惩罚多余变量；调整$R^2_{adj}$会根据自由度惩罚加入无意义变量。
